In v3/index.html, inside the Commitment Readiness section (Section 1),
the Model selector must be a single <select> dropdown — NOT checkboxes.

Replace the current checkbox model selector with this exact HTML:

<select id="commitment-model">
  <option value="gpt-4.1-2025-04-14" selected>gpt-4.1-2025-04-14</option>
  <option value="gpt-4o">gpt-4o</option>
  <option value="gpt-4o-mini">gpt-4o-mini</option>
  <option value="o3">o3</option>
  <option value="o4-mini">o4-mini</option>
</select>

Style it exactly like the Region dropdown in the filter bar.

Then make sure assessCommitmentReadiness() reads:
  region from the Region dropdown in filter bar
  deployType from the Deploy Type dropdown in filter bar  
  model from this new commitment-model dropdown
  requestedPTUs from the PTU input field

All four inputs together → look up single MOCK_CAPACITY row → 
compute GO / CAUTION / NO-GO.

Save the file.

In v3/index.html, the Model dropdown in Commitment Readiness section 
is not triggering a recalculation when changed.

Fix: add an event listener on the commitment-model dropdown so that 
whenever its value changes, it immediately calls refreshCommitmentReadiness()
or re-runs assessCommitmentReadiness() with the new model value.

The same way the Region and Deploy Type dropdowns trigger a dashboard refresh
when changed — the Model dropdown must do the same for Section 1.

Save the file.
Fix v3/index.html — filter and section logic corrections:

1. REGION filter: change to single-select (radio buttons or single dropdown).
   Only one region can be selected at a time.

2. MODEL filter: remove from main filter bar.
   Model is only relevant for Capacity Heatmap (Section 2).
   Add model selector INSIDE Section 2 only, as a local filter.
   Reason: PTU quota is model-independent
   Source: https://learn.microsoft.com/en-us/azure/foundry/openai/
   how-to/provisioned-throughput-onboarding

3. DEPLOY TYPE filter: change to single-select.
   Update options to match actual Foundry portal (from screenshot):
   - Global Standard
   - Data Zone Standard
   - Standard (Regional)
   - Global Provisioned-managed
   - Data Zone Provisioned-managed
   - Provisioned-managed (Regional)
   Only one can be selected at a time.

4. SECTION ORDER — reorder to:
   Section 1: Commitment Readiness (PTU only — per selected region + deploy type)
   Section 2: Capacity Heatmap (PTU physical availability — model filter local here)
   Section 3: Deployed → Utilized (PTU actual deployment + utilization)
   Section 4: Standard Actual Usage (Global Standard + Data Zone + Regional — actual TPM only, NO quota)
   Section 5: Demand Signal

5. Section 4 Standard usage:
   Show three sub-tables in order:
   a) Global Standard — global aggregate TPM (note: cannot split per region)
   b) Data Zone Standard — per data zone (US / EU)
   c) Regional Standard — per region (matches selected region filter)
   Each row: Model | Actual Input TPM | Actual Output TPM | Throttle % | Trend
   NO quota numbers anywhere.

6. Commitment Readiness (Section 1):
   Inputs: selected Region (from filter) + selected Deploy Type (from filter)
           + user-entered Requested PTUs
   Logic: check MOCK_CAPACITY for that region/deployType combination
   Output: GO / CAUTION / NO-GO banner with generated text
   Note: if Deploy Type is Standard (not PTU), show message:
   "Commitment Readiness applies to PTU deploy types only.
    Standard deployments are pay-as-you-go — no capacity commitment needed."